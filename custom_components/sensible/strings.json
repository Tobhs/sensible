{
  "config": {
    "step": {
      "user": {
        "title": "Add a Sensible sensor",
        "description": "Pick what kind of sensor to add. You can add the integration again for each extra sensor you want.",
        "data": {
          "name": "Name",
          "type": "Sensor type"
        }
      },
      "configure": {
        "title": "Configure: {module}",
        "data": {
          "timezone": "Timezone",
          "bedtime": "Their bedtime (optional, e.g. 23:00)",
          "wake_time": "Their wake-up time (optional, e.g. 07:00)",
          "work_time": "When they start work (optional, e.g. 09:00)",
          "latitude": "Latitude",
          "longitude": "Longitude",
          "language": "Language",
          "api_key": "NASA API key (optional; blank uses a shared demo key)",
          "base": "Base currency (e.g. EUR)",
          "target": "Target currency (e.g. USD)",
          "country": "Country code (e.g. US, GB, DE)"
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Options: {module}",
        "data": {
          "timezone": "Timezone",
          "bedtime": "Their bedtime (optional, e.g. 23:00)",
          "wake_time": "Their wake-up time (optional, e.g. 07:00)",
          "work_time": "When they start work (optional, e.g. 09:00)",
          "latitude": "Latitude",
          "longitude": "Longitude",
          "language": "Language",
          "api_key": "NASA API key (optional; blank uses a shared demo key)",
          "base": "Base currency (e.g. EUR)",
          "target": "Target currency (e.g. USD)",
          "country": "Country code (e.g. US, GB, DE)"
        }
      }
    }
  }
}
